/**
 * Dottie — Aurora UI primitives (design-v2) barrel.
 *
 * The glass/clay/aurora building blocks that read from the active mood palette
 * (see `src/theme`). All Reanimated-backed, Reduce-Motion aware, and written
 * against the `.claude/skills/animate-expo` recipes.
 *
 * ⚠️ design-v2 / UNVERIFIED — authored without a device; verify render + motion
 * on a Node machine. Real frosted glass additionally needs `expo-blur`.
 */

export { AuroraBackground } from './AuroraBackground';
export type { AuroraBackgroundProps } from './AuroraBackground';

export { GlassCard } from './GlassCard';
export type { GlassCardProps } from './GlassCard';

export { ClayButton } from './ClayButton';
export type { ClayButtonProps } from './ClayButton';

export { GlowRing } from './GlowRing';
export type { GlowRingProps } from './GlowRing';

export { AuroraTabBar } from './AuroraTabBar';
export type { AuroraTabBarProps } from './AuroraTabBar';
