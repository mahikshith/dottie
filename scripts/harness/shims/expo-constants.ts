/**
 * Dottie — expo-constants shim (harness only). Just the version fields the
 * report store reads.
 */
export default {
  expoConfig: { version: '0.12.0', name: 'Dottie' },
  manifest: { version: '0.12.0' },
  nativeAppVersion: '0.12.0',
  nativeBuildVersion: '1',
};
